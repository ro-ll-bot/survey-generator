import React from 'react';
import { render, fireEvent } from '@testing-library/react';

describe("Survey Generator Template", () => {
    test('Should create a survey template', () => {
        expect(2).toEqual(2);
    }) ;
});